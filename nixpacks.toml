[phases.setup]
nixPkgs = ["php81", "php81Extensions.pdo_mysql", "php81Extensions.curl", "php81Extensions.mbstring", "php81Packages.composer"]

[phases.build]
cmds = ["composer install --no-dev --ignore-platform-reqs"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
